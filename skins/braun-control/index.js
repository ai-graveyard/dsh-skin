/** Host half: its presence lets the loader discover this package's dsh.client declaration. */
export const name = 'dsh-skin-braun-control'

/** The visual work is intentionally confined to the browser client half. */
export function apply() {}

